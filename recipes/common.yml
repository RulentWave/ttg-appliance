modules:
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /

  - type: dnf
    repos:
      files:
        - tailscale.repo
      keys:
        - keys/tailscale-fedora-repo.gpg
    install:
      packages:
        - clevis
        - clevis-luks
        - clevis-dracut
        - clevis-udisks2
        - clevis-systemd
        - NetworkManager-tui
        - fping
        - btop
        - btrfsmaintenance
        - duperemove
        - inotify-tools
        - udisks2
        - wireshark-cli
        - tailscale
        - dos2unix
        - tayga
        - radvd
        - traceroute
        - smartmontools
        - nmap
        - python3-dnslib
        - helix
        - python3-netifaces
        - https://github.com/Tera-Tech-Group/nat64dns/releases/download/v0.1.0-alpha-3/nat64dns-0.1.0-1.fc44.x86_64.rpm
  - type: systemd
    system:
      enabled:
        - btrfs-scrub.timer
        - btrfs-trim.timer
        - rpm-ostreed-automatic.timer
        - podman-auto-update.timer
        - btrfs-scrub.timer
      disabled:
        - zincati.service
  - type: signing # this sets up the proper policy & signing files for signed images to work fully
