[Timer]
OnCalendar=*-*-* 00:00         # once a day at midnight
RandomizedDelaySec=2h          # randomize within 2h, so it runs between 00:00–02:00
Persistent=true                # make sure it runs even if the system was down
